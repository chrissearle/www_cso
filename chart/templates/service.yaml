apiVersion: v1
kind: Service
metadata:
  name: {{ include "wwwcso.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "wwwcso.labels" . | nindent 4 }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.deployment.port }}
  selector:
    {{- include "wwwcso.selectorLabels" . | nindent 4 }}
